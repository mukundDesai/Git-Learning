git config --global origin 
git init 
git clone
git add (filenames seperated by space or to add all .)
git commit -m "message"
git remote add origin ssh
git push origin main/master/branch
git branch branchname
git checkout branchname
git checkout master
git merge

when u change branch or when it is the first push in that branch use -u (it means --set-upstream)
git push -u origin branch

branching is important

